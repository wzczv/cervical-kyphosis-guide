# Permanent redirects for the old English prefix after English became root.
# Cloudflare Pages parses this from the publish root.
# Netlify force redirects are mirrored in netlify.toml because legacy /en files exist.
/en / 301
/en/ / 301
/en/* /:splat 301
